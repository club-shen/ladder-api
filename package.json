{
	"name": "shn-ladder-api",
	"version": "0.1.0",
	"description": "An API for our ladder system.",

	"main": "bin/main.js",

	"bugs": {
		"url": "https://github.com/club-shen/shn-ladder-api/issues"
	},

	"scripts": {
		"preinstall": "npm install coffee-script",
		"postinstall": "coffee -o bin -c src"
	}
}
